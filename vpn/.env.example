OPENVPN_USERNAME=
OPENVPN_PASSWORD=
# IP LAN de l'hôte sur laquelle publier le proxy HTTP (Privoxy, port 8118) qui
# sort par le tunnel. Vide = 127.0.0.1, joignable depuis l'hôte seulement.
# Jamais 0.0.0.0 : le proxy n'a aucune authentification.
WEBPROXY_HOST_IP=
