# Copy this file to .env and fill in the API keys generated by each app on
# its first start (Settings > General in Prowlarr/Sonarr/Radarr's web UI) —
# see README.md. cross-seed and recyclarr read these back out of the
# container environment (arr/cross-seed/config.js, arr/recyclarr/recyclarr.yml).
PROWLARR_API_KEY=
SONARR_API_KEY=
RADARR_API_KEY=

# Générée par cross-seed (`docker compose ... exec cross-seed cross-seed
# api-key`) — utilisée par arr/scripts/cross-seed-notify.sh (Custom Script
# Connect Sonarr/Radarr) pour déclencher cross-seed dès qu'un import se termine.
CROSSSEED_API_KEY=

# Facultatif — scripts/torrent-cleanup.py et scripts/transmission-stats.py
# résolvent le nom de tracker d'un torrent via les indexeurs Prowlarr
# (indexerUrls/legacyUrls), mais un indexeur public sans tracker qui lui
# soit propre (ex. Nyaa.si, qui n'utilise que des trackers publics
# génériques partagés avec n'importe quel autre torrent) ne peut pas être
# déduit ainsi. Alias manuel : domaine=Nom séparés par des virgules, une
# entrée par domaine de tracker BitTorrent réel (pas le domaine du site).
# Propre à quels trackers publics ton/tes indexeur(s) utilisent en
# pratique — vérifier via les trackerStats de quelques-uns de ses torrents
# dans Transmission avant de remplir.
# TRACKER_ALIASES=tracker1.example.com=MonIndexeur,tracker2.example.com=MonIndexeur
